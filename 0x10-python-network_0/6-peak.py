#!/usr/bin/python
"""find a peak in a list of unsorted integer"""

def find_peak(list_of_integers):

    the_size = len(list_of_integers)
    mid_num = size
    mid_size = size // 2

    if the_size == 0:
        return None


    mid_num = size // 2
    if (mid_size < size - 1 
    return num
